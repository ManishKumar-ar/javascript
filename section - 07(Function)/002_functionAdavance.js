const greet = function greetFunction() {
    console.log("Hello, World!");
};


// Here, the function has a name greetFunction, 
// but you still call it using the variable name greet.
// The name greetFunction is not needed,
// unless you want to refer to the function inside itself (like for recursion or debugging).